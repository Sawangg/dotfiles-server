---
apiVersion: v1
kind: Service
metadata:
  name: searxng-sv
  namespace: searxng
spec:
  type: LoadBalancer
  ipFamilyPolicy: PreferDualStack
  selector:
    app: searxng
  ports:
    - port: 80
      targetPort: 8080
